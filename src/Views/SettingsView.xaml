<UserControl x:Class="Illustra.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Illustra.Views"
             xmlns:vm="clr-namespace:Illustra.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{DynamicResource String_Settings_Title}"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1"
                    Margin="0,10">
            <TextBlock Text="{DynamicResource String_Settings_Language}"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,0,0,10"/>

            <ComboBox x:Name="LanguageComboBox"
                      Width="200"
                      HorizontalAlignment="Left"
                      Margin="0,0,0,20"
                      SelectedIndex="{Binding SelectedLanguageIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <ComboBoxItem Content="{DynamicResource String_Lang_English}"
                              Tag="en"/>
                <ComboBoxItem Content="{DynamicResource String_Lang_Japanese}"
                              Tag="ja"/>
            </ComboBox>

            <TextBlock Text="{DynamicResource String_Settings_LanguageDescription}"
                       Foreground="Gray"
                       TextWrapping="Wrap"
                       Margin="0,0,0,20"/>

            <!-- 新しい設定項目 -->
            <TextBlock Text="Web UI URL" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <TextBox Text="{Binding WebUIUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="400" HorizontalAlignment="Left" Margin="0,0,0,20"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 他の設定項目 -->
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
