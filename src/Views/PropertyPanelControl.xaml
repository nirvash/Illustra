<UserControl x:Class="Illustra.Views.PropertyPanelControl"
             x:Name="PropertyPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Illustra.Views"
             xmlns:controls="clr-namespace:Illustra.Controls"
             xmlns:helpers="clr-namespace:Illustra.Helpers"
             FontSize="{DynamicResource AppFontSize}"
             mc:Ignorable="d"
             MaxHeight="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Window}}">
        <UserControl.Resources>
                <helpers:DateTimeToVisibilityConverter x:Key="DateTimeToVisibilityConverter"/>
                <helpers:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
                <helpers:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
                <helpers:NumberToVisibilityConverter x:Key="NumberToVisibilityConverter"/>
                <helpers:AndMultiValueConverter x:Key="AndMultiValueConverter"/>
                <helpers:BooleanInverseConverter x:Key="BooleanInverseConverter"/>
                <!-- 基本テキストスタイル -->
                <Style x:Key="PropertyPanelTextStyle">
                    <Setter Property="Control.FontSize" Value="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                </Style>

                <!-- すべてのTextBlockに適用される基本スタイル -->
                <Style TargetType="TextBlock" BasedOn="{StaticResource PropertyPanelTextStyle}"/>

                <Style x:Key="NormalTagTextBoxStyle"
                       TargetType="TextBox"
                       BasedOn="{StaticResource PropertyPanelTextStyle}">
                        <Setter Property="IsReadOnly"
                                Value="True"/>
                        <Setter Property="Background"
                                Value="Transparent"/>
                        <Setter Property="BorderThickness"
                                Value="0"/>
                        <Setter Property="TextWrapping"
                                Value="Wrap"/>
                        <Setter Property="Margin"
                                Value="0"/>
                        <Setter Property="Padding"
                                Value="0"/>
                        <Setter Property="SelectionBrush"
                                Value="#666666"/>
                </Style>
                <Style x:Key="LoRATagTextBoxStyle"
                       TargetType="TextBox"
                       BasedOn="{StaticResource PropertyPanelTextStyle}">
                        <Setter Property="IsReadOnly"
                                Value="True"/>
                        <Setter Property="Background"
                                Value="Transparent"/>
                        <Setter Property="BorderThickness"
                                Value="0"/>
                        <Setter Property="TextWrapping"
                                Value="Wrap"/>
                        <Setter Property="Margin"
                                Value="0"/>
                        <Setter Property="Padding"
                                Value="0"/>
                        <Setter Property="SelectionBrush"
                                Value="#90C49A"/>
                </Style>
                <Style x:Key="NegativeTagTextBoxStyle"
                       TargetType="TextBox"
                       BasedOn="{StaticResource PropertyPanelTextStyle}">
                        <Setter Property="IsReadOnly"
                                Value="True"/>
                        <Setter Property="Background"
                                Value="Transparent"/>
                        <Setter Property="BorderThickness"
                                Value="0"/>
                        <Setter Property="TextWrapping"
                                Value="Wrap"/>
                        <Setter Property="Margin"
                                Value="0"/>
                        <Setter Property="Padding"
                                Value="0"/>
                        <Setter Property="SelectionBrush"
                                Value="#E4B5B9"/>
                </Style>

                <!-- 読み取り専用のテキストボックススタイル -->
                <Style x:Key="ReadOnlyTextBoxStyle"
                       TargetType="TextBox"
                       BasedOn="{StaticResource PropertyPanelTextStyle}">
                        <Setter Property="IsReadOnly"
                                Value="True"/>
                        <Setter Property="Focusable"
                                Value="True"/>
                        <Setter Property="Background"
                                Value="Transparent"/>
                        <Setter Property="BorderThickness"
                                Value="0"/>
                        <Setter Property="TextWrapping"
                                Value="Wrap"/>
                        <Setter Property="Margin"
                                Value="0,0,0,5"/>
                </Style>
        </UserControl.Resources>
        <ScrollViewer x:Name="PropertiesScrollViewer"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      CanContentScroll="False"
                      PanningMode="VerticalOnly">
                <Grid x:Name="PropertiesGrid"
                      Margin="10,10,5,10">
                        <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- ファイル名 -->
                        <StackPanel x:Name="FileNameSection" Grid.Row="0">
                            <TextBox Text="{Binding FileName, Mode=OneWay}"
                                FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                FontWeight="Bold"
                                Margin="0,0,0,10"
                                Style="{StaticResource ReadOnlyTextBoxStyle}"/>
                        </StackPanel>

                        <!-- レーティング -->
                        <StackPanel x:Name="RatingSection"
                                    Grid.Row="1"
                                    Orientation="Horizontal"
                                    Margin="0,0,0,10">
                                <TextBlock Text="{DynamicResource String_Property_Rating}"
                                           VerticalAlignment="Center"
                                           FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           Margin="0,0,35,0"/>
                                <Button x:Name="RatingStar1"
                                        Style="{DynamicResource Illustra.Styles.RatingStarButton}"
                                        Tag="1"
                                        Focusable="False"
                                        Command="{Binding RatingCommand, ElementName=PropertyPanel}"
                                        CommandParameter="1">
                                        <controls:RatingStarControl RatingValue="1"
                                                                    Height="18"
                                                                    Width="18"
                                                                    DisplayMode="Multiple"
                                                                    CurrentRating="{Binding Rating, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </Button>
                                <Button x:Name="RatingStar2"
                                        Style="{DynamicResource Illustra.Styles.RatingStarButton}"
                                        Tag="2"
                                        Focusable="False"
                                        Command="{Binding RatingCommand, ElementName=PropertyPanel}"
                                        CommandParameter="2">
                                        <controls:RatingStarControl RatingValue="2"
                                                                    Height="18"
                                                                    Width="18"
                                                                    DisplayMode="Multiple"
                                                                    CurrentRating="{Binding Rating, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </Button>
                                <Button x:Name="RatingStar3"
                                        Style="{DynamicResource Illustra.Styles.RatingStarButton}"
                                        Tag="3"
                                        Focusable="False"
                                        Command="{Binding RatingCommand, ElementName=PropertyPanel}"
                                        CommandParameter="3">
                                        <controls:RatingStarControl RatingValue="3"
                                                                    Height="18"
                                                                    Width="18"
                                                                    DisplayMode="Multiple"
                                                                    CurrentRating="{Binding Rating, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </Button>
                                <Button x:Name="RatingStar4"
                                        Style="{DynamicResource Illustra.Styles.RatingStarButton}"
                                        Tag="4"
                                        Focusable="False"
                                        Command="{Binding RatingCommand, ElementName=PropertyPanel}"
                                        CommandParameter="4">
                                        <controls:RatingStarControl RatingValue="4"
                                                                    Height="18"
                                                                    Width="18"
                                                                    DisplayMode="Multiple"
                                                                    CurrentRating="{Binding Rating, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </Button>
                                <Button x:Name="RatingStar5"
                                        Style="{DynamicResource Illustra.Styles.RatingStarButton}"
                                        Tag="5"
                                        Focusable="False"
                                        Command="{Binding RatingCommand, ElementName=PropertyPanel}"
                                        CommandParameter="5">
                                        <controls:RatingStarControl RatingValue="5"
                                                                    Height="18"
                                                                    Width="18"
                                                                    DisplayMode="Multiple"
                                                                    CurrentRating="{Binding Rating, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </Button>
                        </StackPanel>
                        <!-- 生成メタデータ コピーボタン (上部に常設) -->
                        <WrapPanel x:Name="GenerationCopyButtonsSection"
                                   Grid.Row="2"
                                   Margin="0,0,0,10"
                                   Visibility="{Binding HasGenerationMetadata, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Button Content="{DynamicResource String_Thumbnail_CopyPrompt}"
                                        Padding="10,4"
                                        Margin="0,0,5,0"
                                        Click="CopyGenerationPrompt_Click"
                                        Visibility="{Binding GenerationMetadata.Prompt, Converter={StaticResource StringToVisibilityConverter}}"/>
                                <Button Content="{DynamicResource String_Thumbnail_CopyNegativePrompt}"
                                        Padding="10,4"
                                        Margin="0,0,5,0"
                                        Click="CopyGenerationNegativePrompt_Click"
                                        Visibility="{Binding GenerationMetadata.NegativePrompt, Converter={StaticResource StringToVisibilityConverter}}"/>
                        </WrapPanel>

                        <!-- 詳細情報（折りたたみ可能） -->
                        <Expander x:Name="DetailsSection"
                                Grid.Row="3"
                                IsExpanded="{Binding FolderPathExpanded, Mode=TwoWay}"
                                Header="{DynamicResource String_Property_DetailInfo}"
                                FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                Margin="0,0,0,10">
                                <Grid>
                                        <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/> <!-- Added row for Exposure Info -->
                                        </Grid.RowDefinitions>

                                        <!-- ファイルサイズ -->
                                        <TextBlock Grid.Row="0"
                                                   Grid.Column="0"
                                                   Text="{DynamicResource String_Property_Size}"
                                                   Margin="0,0,10,5"
                                                   Visibility="{Binding FileSizeFormatted, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        <TextBox Grid.Row="0"
                                                 Grid.Column="1"
                                                 Text="{Binding FileSizeFormatted, Mode=OneWay}"
                                                 Style="{StaticResource ReadOnlyTextBoxStyle}"
                                                 Visibility="{Binding FileSizeFormatted, Converter={StaticResource StringToVisibilityConverter}}"/>

                                        <!-- 作成日時 -->
                                        <TextBlock Grid.Row="1"
                                                   Grid.Column="0"
                                                   Text="{DynamicResource String_Property_Created}"
                                                   Margin="0,0,10,5"
                                                   Visibility="{Binding CreatedDate, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        <TextBox Grid.Row="1"
                                                 Grid.Column="1"
                                                 Text="{Binding CreatedDate, Mode=OneWay}"
                                                 Style="{StaticResource ReadOnlyTextBoxStyle}"
                                                 Visibility="{Binding CreatedDate, Converter={StaticResource StringToVisibilityConverter}}"/>

                                        <!-- 更新日時 -->
                                        <TextBlock Grid.Row="2"
                                                   Grid.Column="0"
                                                   Text="{DynamicResource String_Property_Modified}"
                                                   Margin="0,0,10,5"
                                                   Visibility="{Binding ModifiedDate, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        <TextBox Grid.Row="2"
                                                 Grid.Column="1"
                                                 Text="{Binding ModifiedDate, Mode=OneWay}"
                                                 Style="{StaticResource ReadOnlyTextBoxStyle}"
                                                 Visibility="{Binding ModifiedDate, Converter={StaticResource StringToVisibilityConverter}}"/>

                                        <!-- 解像度 -->
                                        <TextBlock Grid.Row="3"
                                                   Grid.Column="0"
                                                   Text="{DynamicResource String_Property_Resolution}"
                                                   Margin="0,0,10,5"
                                                   Visibility="{Binding Resolution, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        <TextBox Grid.Row="3"
                                                 Grid.Column="1"
                                                 Text="{Binding Resolution, Mode=OneWay}"
                                                 Style="{StaticResource ReadOnlyTextBoxStyle}"
                                                 Visibility="{Binding Resolution, Converter={StaticResource StringToVisibilityConverter}}"/>


                                        <!-- 再生時間 (動画のみ) -->
                                        <TextBlock Grid.Row="5"
                                                   Grid.Column="0"
                                                   Text="{DynamicResource String_Property_Duration}"
                                                   Margin="0,0,10,5"
                                                   Visibility="{Binding IsVideo, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        <TextBox Grid.Row="5"
                                                 Grid.Column="1"
                                                 Text="{Binding DurationFormatted, Mode=OneWay}"
                                                 Style="{StaticResource ReadOnlyTextBoxStyle}"
                                                 Visibility="{Binding IsVideo, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                        <!-- 画像形式 -->
                                        <TextBlock Grid.Row="6"
                                                   Grid.Column="0"
                                                   Text="{DynamicResource String_Property_ImageFormat}"
                                                   Margin="0,0,10,5"
                                                   Visibility="{Binding ImageFormat, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        <TextBox Grid.Row="6"
                                                 Grid.Column="1"
                                                 Text="{Binding ImageFormat, Mode=OneWay}"
                                                 Style="{StaticResource ReadOnlyTextBoxStyle}"
                                                 Visibility="{Binding ImageFormat, Converter={StaticResource StringToVisibilityConverter}}"/>

                                        <!-- 色深度 -->
                                        <TextBlock Grid.Row="7"
                                                   Grid.Column="0"
                                                   Text="{DynamicResource String_Property_ColorDepth}"
                                                   Margin="0,0,10,5"
                                                   Visibility="Collapsed"/>
                                        <TextBox Grid.Row="7"
                                                 Grid.Column="1"
                                                 Text="{Binding ColorDepth, Mode=OneWay}"
                                                 Style="{StaticResource ReadOnlyTextBoxStyle}"
                                                 Visibility="Collapsed"/>

                                        <!-- 撮影日時 -->
                                        <TextBlock Grid.Row="8"
                                                   Grid.Column="0"
                                                   Text="{DynamicResource String_Property_DateTaken}"
                                                   Margin="0,0,10,5"
                                                   Visibility="{Binding DateTaken, Converter={StaticResource DateTimeToVisibilityConverter}}"/>
                                        <TextBox Grid.Row="8"
                                                 Grid.Column="1"
                                                 Text="{Binding DateTaken, StringFormat=yyyy/MM/dd HH:mm:ss, Mode=OneWay}"
                                                 Style="{StaticResource ReadOnlyTextBoxStyle}"
                                                 Visibility="{Binding DateTaken, Converter={StaticResource DateTimeToVisibilityConverter}}"/>

                                        <!-- カメラ情報 -->
                                        <TextBlock Grid.Row="9"
                                                   Grid.Column="0"
                                                   Text="{DynamicResource String_Property_Camera}"
                                                   Margin="0,0,10,5"
                                                   Visibility="{Binding CameraModel, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        <TextBox Grid.Row="9"
                                                 Grid.Column="1"
                                                 Text="{Binding CameraModel, Mode=OneWay}"
                                                 Style="{StaticResource ReadOnlyTextBoxStyle}"
                                                 Visibility="{Binding CameraModel, Converter={StaticResource StringToVisibilityConverter}}"/>

                                        <!-- 撮影情報 -->
                                        <TextBlock Grid.Row="10"
                                                   Grid.Column="0"
                                                   Text="{DynamicResource String_Property_ExposureInfo}"
                                                   Margin="0,0,10,5"
                                                   Visibility="{Binding ExposureTime, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        <StackPanel Grid.Row="10"
                                                    Grid.Column="1"
                                                    Margin="0,0,0,5"
                                                    Visibility="{Binding ExposureTime, Converter={StaticResource StringToVisibilityConverter}}">
                                                <TextBox Text="{Binding ExposureTime, Mode=OneWay}"
                                                         Style="{StaticResource ReadOnlyTextBoxStyle}"/>
                                                <TextBox Text="{Binding FNumber, Mode=OneWay}"
                                                         Style="{StaticResource ReadOnlyTextBoxStyle}"/>
                                                <TextBox Text="{Binding ISOSpeed, Mode=OneWay}"
                                                         Style="{StaticResource ReadOnlyTextBoxStyle}"/>
                                        </StackPanel>
                                </Grid>
                        </Expander>

                        <!-- Stable Diffusion情報 -->
                        <Expander x:Name="StableDiffusionSection"
                                  Grid.Row="4"
                                  Header="{DynamicResource String_Property_StableDiffusion}"
                                  Margin="0,0,0,10"
                                  IsExpanded="{Binding StableDiffusionExpanded}"
                                  Visibility="{Binding HasStableDiffusionData, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                        <!-- モデル情報 -->
                                        <Grid Margin="0,5,0,5">
                                                <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0"
                                                           Text="{DynamicResource String_Property_Model}"
                                                           Margin="0,0,10,5"/>
                                                <TextBox Grid.Column="1"
                                                         PreviewMouseRightButtonDown="Tag_PreviewMouseRightButtonDown"
                                                         Text="{Binding StableDiffusionResult.Model, Mode=OneWay}"
                                                         Style="{StaticResource ReadOnlyTextBoxStyle}">
                                                    <TextBox.ContextMenu>
                                                        <ContextMenu>
                                                            <MenuItem Header="{DynamicResource String_Menu_CopyText}"
                                                                    Click="CopyTag_Click"/>
                                                        </ContextMenu>
                                                    </TextBox.ContextMenu>
                                                </TextBox>
                                        </Grid>

                                        <!-- プロンプトタグ -->
                                        <TextBlock Text="{DynamicResource String_Property_Prompt}"
                                                   Margin="0,5,0,5"/>
                                        <ItemsControl ItemsSource="{Binding StableDiffusionResult.Tags}"
                                                      x:Name="TagsItemsControl">
                                                <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                                <WrapPanel/>
                                                        </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                                <Border Background="#D1E3FF"
                                                                        BorderThickness="1"
                                                                        BorderBrush="Transparent"
                                                                        Padding="5,2"
                                                                        Margin="0,0,5,5"
                                                                        CornerRadius="3">
                                                                        <TextBox Text="{Binding Path=., Mode=OneWay}"
                                                                                 Foreground="Black"
                                                                                 Style="{StaticResource NormalTagTextBoxStyle}"
                                                                                 IsReadOnlyCaretVisible="True"
                                                                                 VerticalAlignment="Center"
                                                                                 MinWidth="20"
                                                                                 Tag="{Binding Path=.}"

                                                                                 PreviewMouseRightButtonDown="Tag_PreviewMouseRightButtonDown"
                                                                                 Loaded="Tag_Loaded">
                                                                                <TextBox.ContextMenu>
                                                                                        <ContextMenu>
                                                                                                <MenuItem Header="{DynamicResource String_Menu_CopyText}"
                                                                                                        Click="CopyTag_Click"
                                                                                                        Tag="{Binding}"/>
                                                                                                <Separator/>
                                                                                                <MenuItem Header="{DynamicResource String_Menu_AddTagToFilter}"
                                                                                                        Click="AddTagToFilter_Click"
                                                                                                        Tag="{Binding}"/>
                                                                                                <MenuItem Header="{DynamicResource String_Menu_RemoveTagFromFilter}"
                                                                                                        Click="RemoveTagFromFilter_Click"
                                                                                                        Tag="{Binding}"/>
                                                                                        </ContextMenu>
                                                                                </TextBox.ContextMenu>
                                                                        </TextBox>
                                                                </Border>
                                                        </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <!-- LoRAタグ -->
                                        <TextBlock Text="{DynamicResource String_Property_LoRA}"
                                                   Margin="0,10,0,5"
                                                   Visibility="{Binding StableDiffusionResult.Loras.Count, Converter={StaticResource NumberToVisibilityConverter}}"/>
                                        <ItemsControl x:Name="LoraTagsItemsControl" ItemsSource="{Binding StableDiffusionResult.Loras}">
                                                <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                                <WrapPanel/>
                                                        </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                                <Border Background="#D4EDDA"
                                                                        BorderThickness="1"
                                                                        BorderBrush="Transparent"
                                                                        Padding="5,2"
                                                                        Margin="0,0,5,5"
                                                                        CornerRadius="3">
                                                                        <TextBox Text="{Binding Path=.}"
                                                                                Foreground="Black"
                                                                                Style="{StaticResource LoRATagTextBoxStyle}"
                                                                                IsReadOnlyCaretVisible="True"
                                                                                VerticalAlignment="Center"
                                                                                MinWidth="20"
                                                                                Tag="{Binding Path=.}"
                                                                                PreviewMouseRightButtonDown="Tag_PreviewMouseRightButtonDown"
                                                                                Loaded="Tag_Loaded">
                                                                                <TextBox.ContextMenu>
                                                                                        <ContextMenu>
                                                                                                <MenuItem Header="{DynamicResource String_Menu_CopyText}"
                                                                                                        Click="CopyTag_Click"
                                                                                                        Tag="{Binding}"/>
                                                                                                <Separator/>
                                                                                                <MenuItem Header="{DynamicResource String_Menu_AddTagToFilter}"
                                                                                                        Click="AddTagToFilter_Click"
                                                                                                        Tag="{Binding}"/>
                                                                                                <MenuItem Header="{DynamicResource String_Menu_RemoveTagFromFilter}"
                                                                                                        Click="RemoveTagFromFilter_Click"
                                                                                                        Tag="{Binding}"/>
                                                                                        </ContextMenu>
                                                                                </TextBox.ContextMenu>
                                                                        </TextBox>
                                                                </Border>
                                                        </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <!-- Negative Prompt -->
                                        <TextBlock Text="{DynamicResource String_Property_NegativePrompt}"
                                                   Margin="0,10,0,5"/>
                                        <ItemsControl ItemsSource="{Binding StableDiffusionResult.NegativeTags}">
                                                <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                                <WrapPanel/>
                                                        </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                                <Border Background="#F8D7DA"
                                                                        Padding="5,2"
                                                                        Margin="0,0,5,5"
                                                                        CornerRadius="3">
                                                                        <TextBox Text="{Binding Path=.}"
                                                                                 Foreground="Black"
                                                                                 Style="{StaticResource NegativeTagTextBoxStyle}"
                                                                                 IsReadOnlyCaretVisible="True"
                                                                                 VerticalAlignment="Center"
                                                                                 MinWidth="20"
                                                                                 PreviewMouseRightButtonDown="Tag_PreviewMouseRightButtonDown">
                                                                          <TextBox.ContextMenu>
                                                                              <ContextMenu>
                                                                                  <MenuItem Header="{DynamicResource String_Menu_CopyText}"
                                                                                          Click="CopyTag_Click"/>
                                                                              </ContextMenu>
                                                                          </TextBox.ContextMenu>
                                                                      </TextBox>
                                                                </Border>
                                                        </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <!-- コピー/編集ボタン -->
                                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                            <Button Content="{DynamicResource String_Thumbnail_CopyPrompt}"
                                                    Margin="0,0,10,0"
                                                    Padding="10,5"
                                                    Focusable="False"
                                                    Click="CopyPromptContent_Click"/>
                                            <Button Content="{DynamicResource String_Thumbnail_CopyNegativePrompt}"
                                                    Margin="0,0,10,0"
                                                    Padding="10,5"
                                                    Focusable="False"
                                                    Click="CopyNegativePrompt_Click"
                                                    Visibility="{Binding StableDiffusionResult.NegativePrompt, Converter={StaticResource StringToVisibilityConverter}}"/>
                                            <Button Content="{DynamicResource String_Thumbnail_CopyAllPrompt}"
                                                    Margin="0,0,10,0"
                                                    Padding="10,5"
                                                    Focusable="False"
                                                    Click="CopyOriginalText_Click"/>
                                            <Button Content="{DynamicResource String_Property_EditOriginalPrompt}"
                                                    Padding="10,5"
                                                    Focusable="False"
                                                    Click="EditOriginalPrompt_Click"
                                                    Visibility="{Binding CanEditExif, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        </StackPanel>
                                </StackPanel>
                        </Expander>

                        <!-- 生成メタデータ (Stable Diffusion 以外の生成ツール、ComfyUI 動画等) -->
                        <Expander x:Name="GenerationMetadataSection"
                                  Grid.Row="5"
                                  Header="{DynamicResource String_Property_Generation}"
                                  IsExpanded="True"
                                  Margin="0,0,0,10"
                                  Visibility="{Binding HasGenerationMetadata, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                        <!-- 生成ツール -->
                                        <Grid Margin="0,5,0,5"
                                              Visibility="{Binding GenerationMetadata.Generator, Converter={StaticResource StringToVisibilityConverter}}">
                                                <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0"
                                                           Text="{DynamicResource String_Property_GenerationTool}"
                                                           Margin="0,0,10,5"/>
                                                 <TextBox Grid.Column="1"
                                                          Text="{Binding GenerationMetadata.Generator, Mode=OneWay}"
                                                          Style="{StaticResource ReadOnlyTextBoxStyle}">
                                                         <TextBox.ContextMenu>
                                                                 <ContextMenu>
                                                                         <MenuItem Header="{DynamicResource String_Menu_CopyText}"
                                                                                   Tag="Generator"
                                                                                   Click="CopyGenerationField_Click"/>
                                                                 </ContextMenu>
                                                         </TextBox.ContextMenu>
                                                 </TextBox>
                                        </Grid>

                                        <!-- モデル -->
                                        <Grid Margin="0,5,0,5"
                                              Visibility="{Binding GenerationMetadata.ModelName, Converter={StaticResource StringToVisibilityConverter}}">
                                                <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0"
                                                           Text="{DynamicResource String_Property_Model}"
                                                           Margin="0,0,10,5"/>
                                                 <TextBox Grid.Column="1"
                                                          Text="{Binding GenerationMetadata.ModelName, Mode=OneWay}"
                                                          Style="{StaticResource ReadOnlyTextBoxStyle}">
                                                         <TextBox.ContextMenu>
                                                                 <ContextMenu>
                                                                         <MenuItem Header="{DynamicResource String_Menu_CopyText}"
                                                                                   Tag="ModelName"
                                                                                   Click="CopyGenerationField_Click"/>
                                                                 </ContextMenu>
                                                         </TextBox.ContextMenu>
                                                 </TextBox>
                                        </Grid>

                                        <!-- パラメータ (キー: 値 の行で表示) -->
                                        <ItemsControl ItemsSource="{Binding GenerationMetadata.Parameters}"
                                                      Margin="0,5,0,0"
                                                      Visibility="{Binding GenerationMetadata.Parameters.Count, Converter={StaticResource NumberToVisibilityConverter}}">
                                                <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                                <Grid Margin="0,2,0,2">
                                                                        <Grid.ColumnDefinitions>
                                                                                <ColumnDefinition Width="110"/>
                                                                                <ColumnDefinition Width="*"/>
                                                                        </Grid.ColumnDefinitions>
                                                                        <TextBlock Grid.Column="0"
                                                                                   Text="{Binding Key}"
                                                                                   VerticalAlignment="Center"
                                                                                   Opacity="0.8"
                                                                                   ToolTip="{Binding Value}"/>
                                                                        <TextBox Grid.Column="1"
                                                                                 Text="{Binding Value, Mode=OneWay}"
                                                                                 Style="{StaticResource ReadOnlyTextBoxStyle}">
                                                                                <TextBox.ContextMenu>
                                                                                        <ContextMenu>
                                                                                                <MenuItem Header="{DynamicResource String_Menu_CopyText}"
                                                                                                        Click="CopyTag_Click"/>
                                                                                        </ContextMenu>
                                                                                </TextBox.ContextMenu>
                                                                        </TextBox>
                                                                </Grid>
                                                        </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <!-- プロンプト本文 (タグ分解せず長文のまま表示) -->
                                        <TextBlock Text="{DynamicResource String_Property_Prompt}"
                                                   Margin="0,10,0,5"
                                                   Visibility="{Binding GenerationMetadata.Prompt, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        <TextBox Text="{Binding GenerationMetadata.Prompt, Mode=OneWay}"
                                                 Style="{StaticResource ReadOnlyTextBoxStyle}"
                                                 TextWrapping="Wrap"
                                                 VerticalScrollBarVisibility="Auto"
                                                 MaxHeight="200"
                                                 HorizontalScrollBarVisibility="Disabled"
                                                 Visibility="{Binding GenerationMetadata.Prompt, Converter={StaticResource StringToVisibilityConverter}}">
                                                <TextBox.ContextMenu>
                                                        <ContextMenu>
                                                                <MenuItem Header="{DynamicResource String_Menu_CopyText}"
                                                                          Click="CopyGenerationPrompt_Click"/>
                                                        </ContextMenu>
                                                </TextBox.ContextMenu>
                                        </TextBox>

                                        <!-- ネガティブプロンプト -->
                                        <TextBlock Text="{DynamicResource String_Property_NegativePrompt}"
                                                   Margin="0,10,0,5"
                                                   Visibility="{Binding GenerationMetadata.NegativePrompt, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        <TextBox Text="{Binding GenerationMetadata.NegativePrompt, Mode=OneWay}"
                                                 Style="{StaticResource ReadOnlyTextBoxStyle}"
                                                 TextWrapping="Wrap"
                                                 VerticalScrollBarVisibility="Auto"
                                                 MaxHeight="120"
                                                 HorizontalScrollBarVisibility="Disabled"
                                                 Visibility="{Binding GenerationMetadata.NegativePrompt, Converter={StaticResource StringToVisibilityConverter}}">
                                                <TextBox.ContextMenu>
                                                        <ContextMenu>
                                                                <MenuItem Header="{DynamicResource String_Menu_CopyText}"
                                                                          Click="CopyGenerationNegativePrompt_Click"/>
                                                        </ContextMenu>
                                                </TextBox.ContextMenu>
                                        </TextBox>

                                        <!-- LoRA -->
                                        <TextBlock Text="{DynamicResource String_Property_LoRA}"
                                                   Margin="0,10,0,5"
                                                   Visibility="{Binding GenerationMetadata.Loras.Count, Converter={StaticResource NumberToVisibilityConverter}}"/>
                                        <ItemsControl ItemsSource="{Binding GenerationMetadata.Loras}"
                                                      Visibility="{Binding GenerationMetadata.Loras.Count, Converter={StaticResource NumberToVisibilityConverter}}">
                                                <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                                <WrapPanel/>
                                                        </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                                <Border Background="#D4EDDA"
                                                                        BorderThickness="1"
                                                                        BorderBrush="Transparent"
                                                                        Padding="5,2"
                                                                        Margin="0,0,5,5"
                                                                        CornerRadius="3">
                                                                        <TextBox Text="{Binding Path=.}"
                                                                                 Foreground="Black"
                                                                                 Style="{StaticResource LoRATagTextBoxStyle}"
                                                                                 IsReadOnlyCaretVisible="True"
                                                                                 VerticalAlignment="Center"
                                                                                 MinWidth="20"
                                                                                 Tag="{Binding Path=.}"
                                                                                 PreviewMouseRightButtonDown="Tag_PreviewMouseRightButtonDown">
                                                                                <TextBox.ContextMenu>
                                                                                        <ContextMenu>
                                                                                                <MenuItem Header="{DynamicResource String_Menu_CopyText}"
                                                                                                        Click="CopyTag_Click"
                                                                                                        Tag="{Binding}"/>
                                                                                        </ContextMenu>
                                                                                </TextBox.ContextMenu>
                                                                        </TextBox>
                                                                </Border>
                                                        </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <!-- コピー/保存ボタン -->
                                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                            <Button Content="{DynamicResource String_Thumbnail_CopyPrompt}"
                                                    Margin="0,0,10,0"
                                                    Padding="10,5"
                                                    Focusable="False"
                                                    Click="CopyGenerationPrompt_Click"
                                                    Visibility="{Binding GenerationMetadata.Prompt, Converter={StaticResource StringToVisibilityConverter}}"/>
                                            <Button Content="{DynamicResource String_Thumbnail_CopyNegativePrompt}"
                                                    Margin="0,0,10,0"
                                                    Padding="10,5"
                                                    Focusable="False"
                                                    Click="CopyGenerationNegativePrompt_Click"
                                                    Visibility="{Binding GenerationMetadata.NegativePrompt, Converter={StaticResource StringToVisibilityConverter}}"/>
                                            <Button Content="{DynamicResource String_Property_CopyWorkflowJson}"
                                                    Padding="10,5"
                                                    Focusable="False"
                                                    Click="CopyWorkflowJson_Click"
                                                    Visibility="{Binding GenerationMetadata.HasWorkflow, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        </StackPanel>

                                        <!-- 解析失敗時のフォールバック表示:
                                             ワークフローが埋め込まれていることだけでも伝える
                                             (ComfyUI で workflow JSON を開けば内容を確認できる) -->
                                        <TextBlock Text="{DynamicResource String_Property_WorkflowEmbeddedOnly}"
                                                   Margin="0,10,0,0"
                                                   FontStyle="Italic"
                                                   Opacity="0.8"
                                                   TextWrapping="Wrap"
                                                   Visibility="{Binding GenerationMetadata.NeedsWorkflowNotice, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                </StackPanel>
                        </Expander>

                        <!-- プロンプト追加ボタン (Stable Diffusion 形式でないファイルでは非表示) -->
                        <Button Grid.Row="6"
                               Content="{DynamicResource String_Property_AddPrompt}"
                               HorizontalAlignment="Left"
                               Padding="10,5"
                               Margin="0,0,0,10"
                               Click="AddPrompt_Click">
                            <Button.Visibility>
                                <MultiBinding Converter="{StaticResource AndMultiValueConverter}">
                                    <Binding Path="HasStableDiffusionData"
                                             Converter="{StaticResource BooleanInverseConverter}"/>
                                    <Binding Path="CanEditExif"
                                             RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                    <Binding Path="HasGenerationMetadata"
                                             Converter="{StaticResource BooleanInverseConverter}"/>
                                </MultiBinding>
                            </Button.Visibility>
                        </Button>

                        <!-- コメント -->
                        <Grid x:Name="CommentSection"
                              Grid.Row="7"
                              Margin="0,0,0,5">
                                <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="{DynamicResource String_Property_Comment}"
                                           Margin="0,0,10,0"
                                           Visibility="{Binding UserComment, Converter={StaticResource StringToVisibilityConverter}}"/>
                                <TextBox Grid.Column="1"
                                         Text="{Binding UserComment, Mode=OneWay}"
                                         Style="{StaticResource ReadOnlyTextBoxStyle}"
                                         Visibility="{Binding UserComment, Converter={StaticResource StringToVisibilityConverter}}"/>
                        </Grid>
                </Grid>
        </ScrollViewer>
</UserControl>
